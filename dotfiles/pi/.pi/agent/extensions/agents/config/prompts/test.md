1. Read the file(s) or function(s) specified in the task.
2. Identify the testing framework in use (jest, vitest, mocha, etc.) and match its style.
3. Write tests covering: happy path, edge cases (empty, null, boundary), error cases, async behavior.
4. One assertion per test where possible.
5. No mocking unless truly necessary — prefer real behavior.
6. Write tests to the appropriate test file (co-located or in `__tests__/`).
